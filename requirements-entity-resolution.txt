# Entity Resolution Dependencies
# Install with: pip install -r requirements-entity-resolution.txt

# Core dependencies
numpy>=1.21.0
pandas>=1.3.0

# Graph embeddings (PyKEEN)
pykeen>=1.10.0
torch>=2.0.0  # CPU version sufficient for 11K nodes

# Fuzzy string matching
jellyfish>=0.9.0  # Jaro-Winkler similarity

# Entity resolution (Optional - for Splink integration)
# splink>=3.9.0  # Uncomment if using Splink

# Progress bars and utilities
tqdm>=4.65.0

# Existing dependencies (should already be installed)
openai>=1.0.0
pydantic>=2.0.0
python-dotenv>=1.0.0
